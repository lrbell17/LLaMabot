type Query {
  chat(message: String!): String
}

type Mutation {
  startChatSession(userId: String!): ChatSession!
  updateChatSessionTimestamp(sessionId: String!): Boolean!
}

type ChatSession {
  sessionId: ID!
  userId: String!
  startedAt: String
}
